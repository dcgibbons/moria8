#importonce
// Apple IIe HAL contract shim. The contract aggregator lives with the
// Commodore platforms for now; it is platform-neutral (contracts +
// HAL_STATUS_* only). Flag: later promotion of contracts to platforms/hal/.

#import "../../commodore/hal/hal_contract.s"
